apiVersion: v1
kind: Service
metadata:
  name: {{ include "python-3-13.fullname" . }}
  labels:
    {{- include "python-3-13.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "python-3-13.selectorLabels" . | nindent 4 }}
